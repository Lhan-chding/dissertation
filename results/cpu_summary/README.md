# CPU summary location

Canonical compact CPU JSON files are versioned under `artifacts/metrics` and
`artifacts/theory_*`. See `experiments/cpu/summaries/README.md` for the closed
public inventory. Per-example outputs are deliberately excluded from Git.

# CPU experiments

The retained CPU runners live in the repository-level `scripts/` directory and
their frozen configurations live under `configs/theory`, `configs/tabular`,
`configs/neural`, and `configs/natural_replay`.

Only compact aggregate evidence is published. Large natural mediator, fork,
image, rollout, and checkpoint artifacts remain ignored.

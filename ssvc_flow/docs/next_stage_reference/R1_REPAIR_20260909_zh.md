# R1 修复记录（2026-09-09）

用户授权继续修复，并于同日授权删除失败产物以减少冗余。本文件保留必要的故障摘要；不替代新服务器验证。

## 已确认故障

- 146461：脚本在同一 export 中引用尚未初始化的 XDG_CACHE_HOME，启动退出1。
- 146464：只读 parity forward 未禁用 autograd，CUDA OOM，退出1。
- 146479：为修正 parity/smoke 模型生命周期而取消。
- 146491（source f11c7e5887afa583ebf483abaa5a65f584691242）：运行49分52秒，退出1。
  - 固定参考120条、1913个生成token，9条缓存/teacher-forcing/batch候选路径均未过数值阈值。
  - mean selected-token logp差0.007565–0.008760；p99差0.105612–0.123949；top1一致率0.991113–0.996864。
  - smoke额外生成旧P1的288条初始样本后，IMAGE_CUE的cache token/chunk top1=0.9375低于0.999；image影响检查通过；optimizer更新0次。
  - status.json/training_smoke.json未生成，异常未进入最终报告流程。

## 修复边界

文档§3允许拒绝提速路径并使用已验证的慢路径。现有生成与训练均使用 uncached_prefix_recompute；不能把未采用的缓存路径失败改写为成功，也不能降低0.02/0.1/0.999阈值。

新增参考路径自身重复、行为概率、eval/train likelihood检查，逐token保存结果；独立R1 smoke最多4×4×8=128条新rollout，实际更新与恢复检查另计重放；I4不执行；失败必须写出完整状态和已完成步骤。后续门禁以新实测为准。

## 本地验证与清理

- `OMP_NUM_THREADS=1 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 ../.venv/bin/python -m pytest tests -q`：374 passed，54.06秒。
- 修改范围的 Ruff 检查及 `git diff --check` 通过。
- 包含官方随机初始化 CPU tiny Qwen 的完整120条 parity 流程、27个固定边界检查和图像顺序/padding测试；不下载模型权重。完整流程测试仅在 fixture 中排除随机生成图像占位符，生产分布不变。
- CPU测试不构成9B/BF16/CUDA训练通过的证据。实际梯度累积比较为逐样本backward与相同逐样本forward的合并loss backward；不宣称批量模型forward已验证。
- 已删除 `NEXT_20260909/R1_server_{146461,146464,146479,146491}_attempt_0`、对应归档及校验文件、对应Slurm日志。服务器重新查询确认无旧R1目录；P1、P3、R0及其他作业保留。

新作业仍须通过参考路径证书、4次实际更新、恢复重放、实现不变量和scratch状态重置后才能判定R1通过。

# 下一步最小工作：先补证据，再做真正的决策比较

本文件是本次结果分析产生的建议，不自动授权启动训练或改写原实验。

## 1. 已有数据的只读分析
从本包compact数据可完成：
- 按原点、端点、关系/接口拆解pX、A、V、C。
- 用 C=pX+C_wrong 区分正确概率和错误输出中的关系质量。
- 对同一奖励向量下的坐标破坏、单处修复和复制行为建立分布。
- 审计哪些“语义特征”已经由均值、方差或协方差决定。
- 保留O1/O2原来的来源和统计episode；不混成两个足够大的训练seed集合。
- 不把本次posthoc paired-scene interval替代冻结Hoeffding决策。

优先阅读 REPORT_zh.md 第3、4、7、8节。

## 2. 下个评估协议先做决定，避免继续无法识别
在再次看结果前确定：
- 主要比较数量（不把所有配方×全部指标×所有look都当成同等主检验）。
- 精确恢复、错误结构及最弱群体分别追求多大差异。
- 固定面板采样推断与场景外推分别使用什么统计量。
- 端点测量、近似等效、非劣保证分别输出何种结论。
- 最大采样量与所选界能否在常见情况达到精度；提前用公式/模拟检查。

不要默认简单追加到512次就能通过旧门槛，也不要为了通过而删掉误差要求。

## 3. 利用尚未读过的E面板复核
使用已保存端点，不需要重新训练原16臂。评估面板和主要比较在读取E之前固定。
重点：关系全满足错误、保留原有正确坐标、pX及qX。
对R3匹配R0；R6匹配R1；保留已有GDPO/SAW/R4端点作为对照。
这一阶段建立独立场景复核，不自动建立跨训练种子结论。

## 4. 后续真正的“信息价值”实验
候选信息层：
- M0：所有可用奖励的均值、方差、协方差；
- M1：同样统计按关系/接口分层；
- M2：再加入不能由上述量恢复的错误位置、正确坐标破坏等结构。
每层的最终决策目标相同，验证信息权限和计算量清楚登记。

只用干预前观测选择操作，用独立干预后数据评估选择。当前只有两个原点，不能据此训练高容量控制器并声称泛化。
不同原点比较时，尽量增加相同训练题序列的配对控制，区分原点状态与训练序列影响。
可先使用计划中的新训练阶段，再决定独立新seed规模。不要按当前最佳点值只保留赢家。

## 5. 工程工作控制
保留实际需要的身份、模型/优化器起点恢复、评估隔离与原始输出。
每阶段记录一次源码/配置版本，重点防止错误复用与重复生成。
不为清理O2/R4历史Slurm异常重跑完整实验。

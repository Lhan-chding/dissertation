# Codex 入口：语义状态能否改善前瞻奖励选择？

日期：2026-09-24。工作名：`prospective-selection-v2`。本包是待执行方案，不是实验结果。

## 任务

在真实 Qwen3.5-9B 上验证：**只使用训练操作执行前的观测，加入无法由完整奖励分布恢复的修复结构，能否比奖励统计基线选择更有效的下一段奖励配置。**

核心产物不是二维图或完整概率预测器，而是经过独立训练原点检验的“信息 → 选择 → 后续收益”证据。

## 阅读顺序

1. `CODEX_EXECUTION_PLAN_zh.md`：实验主规范。
2. `THEORY_STATISTICS_zh.md`：建模对象、统计单位、精度与阈值。
3. `GPU_RUNBOOK_zh.md`：4–5张PRO6000并行执行。
4. `IMPLEMENTATION_ACCEPTANCE_zh.md`：接口与测试。
5. `protocol.json`：默认参数及工作量生成规则。
6. `SOURCES_AND_CODE_MAP.md`：本次核读的源码与原始文献。

## 仓库起点

本次在线核验：

- 仓库：`Lhan-chding/dissertation`。
- 分支：`codex/decision-modeling-v1-20260919`。
- HEAD：`b52f55993791a9954be07a0195eb656441b53651`。
- 重点目录：`ssvc_flow/src/decision_modeling/`。
- 建议新分支：`codex/prospective-reward-selection-20260924`，独立worktree。

开始时检查最新分支一次；有后续修复就记录实际继承版本，不硬退回旧HEAD。保留其他任务的未提交修改和实验目录。本任务不要求合并main。

## 执行边界

先完成实现、针对性CPU测试及一个真实GPU smoke；随后按阶段自动推进已通过技术检查的开发任务。默认最多5个独立单卡GPU作业；只能分配4卡时自动用4个worker。不得因旧文档的CPU-only、2–3卡上限或百万级CPU资格矩阵而停工。

**可以并行**：旧E面板复核、新源轨迹、不同原点/配方、端点评估。

**不能提前运行**：最终测试原点的候选训练，必须在模型、样本量和选择规则冻结并写出该原点的`decision.json`之后。开发GPU全部忙碌时不预跑测试反事实端点“等着以后挑”。

只在下列情况暂停相关任务：缺关键原件、非法数据混用、数值非有限、训练状态无法恢复、实际硬件不足、未获得服务器访问/执行授权。某个加速评分路径未通过时回退已验证路径，不冻结全部研究。

## 简化工程流程

每阶段一次源码版本与配置记录；首次导入原件核验一次。每分支开始验证完整状态，之后轻量检查。不要逐step哈希9B基座，不要每个bank重跑全仓测试，不要反复复制权重和同一份样本。测试与测量原件共享，选择器名称不能导致重复训练同一原点/配方/随机重复。

## 分批交付

- 首批：E复核、状态信息冗余表、前4个开发lineage的响应表、实测吞吐。
- 第二批：完整开发/调参数据、四层选择器及静态/SAW/GDPO/直接语义奖励基线、冻结规则。
- 第三批：独立测试、选择收益及区间、失效与反例、`FINAL_DECISION_zh.md`。

不把看过的D2端点重新称为独立测试，不把每题采样当训练seed，不把UNKNOWN写成safe。

## 可直接复制给Codex的要求

请实际完成代码、测试和真实模型实验，不只生成伪代码。先实现信息权限隔离和前瞻选择，再扩大GPU矩阵。所有信息层使用相同前置样本、相同候选集合和同一后续目标；关键比较为语义错误结构选择器与完整逐题奖励分布选择器。主实验不通过实时试跑全部候选来作出选择，不强制低维，不训练大型通用响应预测器。按本包固定阈值及样本量规划执行，记录实际吞吐、原点、seed、选择、性能与错误结构；不为获得显著性修改最终测试方案。

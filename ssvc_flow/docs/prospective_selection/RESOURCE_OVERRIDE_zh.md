# 用户授权的7卡调度覆盖

2026-09-24，用户明确要求“再增加两张卡去跑，尽可能加快速度”。本任务并发上限由5提高到7个单PRO6000作业，pending与未知提交继续占用名额。该授权覆盖设计包原5卡资源限制。

这是调度资源变更。保留已运行的protocol.json字节及其身份，模型、B4/K8、配方、题序、样本量规划、数据面板、Adam恢复、前瞻选择和冻结门禁不变。protocol.json中的原资源字段保留用于既有证据身份；实际新上限由本记录及显式CLI参数--max-gpu-jobs 7 --available-gpus 7指定，CLI默认5仍保守保留。

训练队列和评估队列都就绪时目标为6训练加1观测/评估；当前只有4个source在训练，新增两槽用于已就绪的P观测，减少后续分支等待。空闲队列不保留闲置卡。继续每小时续跑；不取消现有作业，不改变最终测试的选择先于运行规则，不扩展首4条之外的开发lineage。

部署结果：afd3119通过51项jobs测试并部署。实际新增提交被教师QOS拒绝（MaxSubmitJobsPU=5）。原5个作业不受影响。授权上限7与实际可提交容量5须区分；现用--max-gpu-jobs 7 --available-gpus 5，等待站点配额上调。拒绝意图、错误和核对回执完整保存。
